services:
  cache:
    image: redis:8.4-alpine
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --save 20 1 --loglevel warning --requirepass 1sampai8
    volumes:
      - cache:/data
volumes:
  cache:
    driver: local
